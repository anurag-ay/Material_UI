import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#59cf64",
      light: "#25b0f3",
    },
    secondary: {
      main: "#f20304",
    },
    otherColor: {
      main: "#7e8995",
    },
  },
});
